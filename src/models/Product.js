export class Product{
  constructor(id,name, imagePath, title, price) {
    this.id = id;
    this.name = name;
    this.imagePath = imagePath;
    this.title = title;
    this.price = price;
  }
}
export default Product;