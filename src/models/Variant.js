export class Variant {
  constructor(productID, imagePath, color, size, quantity, title, price) {
    this.productID = productID;
    this.imagePath = imagePath;
    this.color = color;
    this.size = size;
    this.quantity = quantity;
    this.title = title;
    this.price = price;
  }
}
export default Variant;